apiVersion: v1
kind: Service
metadata:
  name: monitoring-system-clusterip
  namespace: monitoring-system
  labels:
    app: monitoring-system
spec:
  type: ClusterIP
  selector:
    app: monitoring-system
  ports:
    - name: http
      port: 80
      targetPort: 8080

