apiVersion: v1
kind: Service
metadata:
  name: monitoring-system-lb
  namespace: monitoring-system
  labels:
    app: monitoring-system
spec:
  type: NodePort
  selector:
    app: monitoring-system
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 32080
